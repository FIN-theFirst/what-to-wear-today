<!DOCTYPE html>
<html lang="de">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    html,
    body {
      height: 100dvh;
      /* dynamische Viewport-Höhe = echte sichtbare Höhe */
      margin: 0;
    }

    body {
      display: flex;
      justify-content: center;
      align-items: center;
    }
  </style>
</head>

<body class="bg-dark">

  <form method="POST" action="/login" class="text-white rounded p-3 w-100 text-center mx-3"
    style="max-width: 360px; background: #337ab7;">

    <h2 class="mb-3 ">Anmeldung</h2>

    <% if (error) { %>
      <div class="alert alert-danger bg-danger border-0 text-white">
        <%= error %>
      </div>
      <% } %>

        <div class="mb-3">
          <input type="text" name="username" class="form-control" placeholder="Benutzername" required>
        </div>
        <div class="mb-3">
          <input type="password" name="password" class="form-control" placeholder="Passwort" required>
        </div>
        <button type="submit" class="btn btn-dark w-100">Login</button>
  </form>

</body>

</html>
